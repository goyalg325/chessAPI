const passport = require('passport');
const User = require('../models/User'); // Import your User model

// Configure Passport strategies here (e.g., local, JWT, etc.)
// passport.use(new YourStrategy());

module.exports = passport;
